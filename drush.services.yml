services:
  content_sync.commands:
    class: \Drupal\content_sync\Commands\ContentSyncCommands
    arguments: ['@content.storage.sync', '@content.storage', '@content_sync.manager']
    tags:
      - { name: drush.command }
